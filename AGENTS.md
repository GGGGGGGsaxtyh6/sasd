## Cursor Cloud specific instructions

This repository is currently a blank canvas with no application code, dependencies, or services. The only tracked file is `sd` (a placeholder).

### Development Environment

- **No dependencies to install** — there are no package manifests, lock files, or build configurations.
- **No services to run** — there are no applications, servers, or background processes.
- **No tests or linting** — there are no test frameworks or linter configurations present.

### When code is added

Once application code and dependency files are added to the repository, update this section and the VM update script accordingly.
